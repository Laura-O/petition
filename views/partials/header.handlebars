<header class="top">
    <h1>Petition</h1>
</header>
<nav class="flex-nav">
    <a href="#" class="toggleNav">
        <i class="fa fa-bars"></i>Menu</a>
    <ul>
        {{#if user}}
        <li>
            <i class="fas fa-user"></i> {{user.first}}
        </li>
        <li>
            <button type="submit" value="Logout">Logout</button>
        </li>
        {{else}}
        <li>
            <a href="login ">Login</a>
        </li>
        <li>
            <a href="register ">Register</a>
        </li>
        {{/if}}
    </ul>
</nav>