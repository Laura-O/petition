<body>
    <div class="container">
        <form class="form" id="info-form" method="POST">
            <input type="hidden" name="_csrf" value="{{csrfToken}}">
            <p>
                <label for="age">First Name</label>
                <input type="text" name="first" value={{user.first}}>
            </p>
            <p>
                <label for="city">Last Name</label>
                <input type="text" name="last" value={{user.last}}>
            </p>
            <p>
                <label for="homepage">Email Address</label>
                <input type="email" name="email" value={{user.email}}>
            </p>
            <p>
                <label for="age">Age</label>
                <input type="number" name="age" value={{user.age}}>
            </p>
            <p>
                <label for="city">City</label>
                <input type="text" name="city" value={{user.city}}>
            </p>
            <p>
                <label for="homepage">Homepage</label>
                <input type="url" name="url" value={{user.url}}>
            </p>
            <p>
                <label for="homepage">Password</label>
                <input type="email" name="pass">
            </p>
            <div>
                <input id="submit" type="submit" value="Save">
            </div>
        </form>
    </div>